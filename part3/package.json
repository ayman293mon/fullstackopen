{
  "name": "part3",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "shx rm -rf dist && cd ../part2/phonebook && npm install && npm run build && shx cp -r dist ../../part3/",
    "deploy:full": "npm run build:ui && git add . && git commit -m \"deploying full app\" && git push origin main"
  },
  "author": "mon",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "mongoose": "^9.1.5",
    "morgan": "^1.10.1"
  },
  "devDependencies": {
    "shx": "^0.4.0"
  }
}
